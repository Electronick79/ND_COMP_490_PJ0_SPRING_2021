__import__('_distutils_hack').do_override()
import requests
from pprint import pprint

import requests as requests

    username = "Electronick79"
    url = f"https://api,github,com/user/{username}"
    user_data = requests.get(url).json()
    pprint(user_data)